# Vault expiry clock source

Release smoke checks should confirm vault expiry copy uses the same network clock source as the contract read.

If wall-clock and block-height estimates disagree, note both values in the handoff.
