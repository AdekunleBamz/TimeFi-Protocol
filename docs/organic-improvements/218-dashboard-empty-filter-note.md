# Dashboard empty filter note

When dashboard filters return no vaults, confirm the empty state suggests clearing filters before reconnecting a wallet.

This avoids mislabeling filter state as a wallet failure.
