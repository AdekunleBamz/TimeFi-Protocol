# Lock Extension Preview Note

Lock extension reviews should confirm the new maturity preview is clear before a transaction is submitted.

## Checks
- Compare the old and new maturity values before opening the wallet prompt.
- Confirm the preview uses the same unit as the contract input.
- Record the vault ID and network when extension math looks wrong.
