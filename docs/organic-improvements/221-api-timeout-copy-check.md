# API timeout copy check

If a vault API request times out, the UI should show retry guidance rather than a generic failure.

Capture timeout duration in the issue so the threshold can be tuned later.
