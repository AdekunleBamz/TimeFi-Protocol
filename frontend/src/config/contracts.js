/**
 * Contract Configuration
 * Re-exporting from @timefi/sdk
 */

export {
    CONTRACT_ADDRESS,
    CONTRACT_NAMES,
    LOCK_PERIODS,
    MIN_DEPOSIT,
    MAX_DEPOSIT
} from '@timefi/sdk';

// Frontend specific config
export const FEE_BPS = 50;
