# Frontend env network note

Review frontend network environment values before deploys so explorer links and read-only calls target the same chain.

## Checklist

- Compare frontend environment values with the deployment plan.
- Rebuild preview deployments after network or contract-address changes.
