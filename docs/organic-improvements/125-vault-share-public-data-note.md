# Vault share public data note

Vault share links should expose only public vault metadata and network context.

Do not include owner private notes, local wallet labels, or API credentials in copied support summaries.

Use the vault ID and the explorer link as the canonical handoff reference.
