# Testnet faucet buffer note

Keep a testnet STX buffer for vault smoke tests so setup does not block release validation.

## Checklist

- Confirm smoke wallets have enough testnet STX before rehearsal.
- Refill the buffer before scheduled deployment windows.
