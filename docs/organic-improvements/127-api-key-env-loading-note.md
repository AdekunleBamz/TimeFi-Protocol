# API key env loading note

When local tools read `.env`, document which variable names are supported and keep the values out of the repository.

Prefer explicit command examples that show placeholders, not real keys.

This prevents troubleshooting notes from becoming a secret exposure path.
