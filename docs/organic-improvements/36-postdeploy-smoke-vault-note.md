# Postdeploy smoke vault note

Create one small smoke vault after deployment so contract, SDK, and frontend reads are verified against the same record.

## Checklist

- Record the smoke vault ID and creation transaction ID.
- Confirm the vault appears in the frontend and SDK read output.
