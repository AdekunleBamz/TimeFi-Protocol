# Vault ID Copy Note

Support flows should make vault IDs easy to copy without requiring users to share full wallet history.

## Checks
- Confirm the full vault ID is available from the vault detail view.
- Verify copied IDs match the format expected by SDK and contract reads.
- Include wallet address only when it is needed to reproduce the lookup.
